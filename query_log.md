```sql
SELECT * FROM unisexdb;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

```sql
INSERT INTO unisexdb 
        (NAME, TOTAL, MALE_SHARE, FEMALE_SHARE, GAP) 
        VALUES ( '7', Alex', 3000, 0.45, 0.55, 0.1);
        
```

```sql
SELECT * FROM unisexdb;
```

```sql
UPDATE unisexdb 
        SET NAME = 'Taylor', TOTAL = 5000, MALE_SHARE = 0.45, 
        FEMALE_SHARE = 0.55, GAP = 0.1
        WHERE ID = 8;
        
```

```sql
DELETE FROM unisexdb WHERE id=9;
```

